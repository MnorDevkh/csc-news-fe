# Backend API base (must include /api/v1 if your FastAPI app mounts there)
# Production: use https://your-api-host/... so TLS matches the site; http is auto-upgraded when the SPA is on HTTPS.
VITE_API_URL=http://127.0.0.1:8000/api/v1
